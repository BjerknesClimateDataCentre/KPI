<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href='..\templates\style.css' type="text/css">
</head>
<body>

  <h1>Data Quality Report</h1>
  <h2> For ICOS station <i>{{station}}</i> - {{df_start}} to {{df_end}}</h2>

  <!-- INTRODUCTION CHAPTER -->
  <h3>1.  Introduction</h3>
    <p>This is a quality assesment of dataset '{{data_filename}}'. Data was
      collected on ICOS station <i>{{station}}</i> between {{df_start}} and
      {{df_end}}. See figures for a quick overview of the data and the data
      quality.</p>

    <figure>
      <img src={{ line_plot_filename}} >
      <figcaption> <strong>Figure x</strong>: The data measured. Colors indicate the quality flag.
      </figcaption>
    </figure>

    <figure>
      <img src={{ stacked_bar_plot_filename}} >
      <figcaption> <strong>Figure x</strong>: The quality of the measured (and calculated
        parameters).
      </figcaption>
    </figure>

    <p> The following sections look closer into the quality of the
      parameters... </p>

    <!-- Add if else statement on which bar plot to show (depending on number
    of parameters)
    <figure>
      <img src={{ kpi_bar_plot_filename}} >
      <figcaption> Figure x: This is the figure caption.
      </figcaption>
    </figure>
    -->


  <!-- PARAMETER CHAPTERS -->
  {% for parameter, config in param_config.items() %}
    <h3> {{loop.index + 1}}.  {{config['chapter_title']}} </h3>
    <p>Some text about this parameter. See figures</p>

    {% for kpi_name, filename in config['kpis'].items() %}
        <figure>
          <img src={{filename}} >
          <figcaption><strong>Figure</strong>: The quality of the measured (and calculated parameters).
          </figcaption>
        </figure>
    {% endfor %}

  {% endfor %}


  <!-- ALTERNATIVE: Extract from child template, but this does not work!
  <div id="line_plot">
    {% block line_plot %}{% endblock %}
  </div>

  <div id="line_plot_cleaned">
    {% block line_plot_cleaned %}{% endblock %}
  </div>

  <div id="bar_plot">
    {% block bar_plot %}{% endblock %}
  </div>
  -->

</body>
</html>
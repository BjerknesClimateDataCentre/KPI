<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href='..\templates\style.css' type="text/css">
</head>
<body>

  <h1>Data Quality Report for {{station}} - {{df_start}} to {{df_end}}</h1>

  <h2>Introduction</h2>
    <p>This is a quality assesment of dataset *datasetname*, collected on ICOS
      station {{station}}. See figures for a quick overview of the data and the
      data quality.</p>

    <figure>
      <img src={{ kpi_line_plot_filename}} >
      <figcaption> Figure x: The data measured. Colors indicate the quality flag.
      </figcaption>
    </figure>

    <figure>
      <img src={{ kpi_stacked_bar_plot_filename}} >
      <figcaption> Figure x: The quality of the measured (and calculated parameters).
      </figcaption>
    </figure>

    <!-- Add if else statement on which bar plot to show (depending on number
    of parameters)
    <figure>
      <img src={{ kpi_bar_plot_filename}} >
      <figcaption> Figure x: This is the figure caption.
      </figcaption>
    </figure>
    -->

  <!-- !!! These chapters needs to be created in a loop to allow for varying
    parameters. For now these test chapters are fine-->

  <h2>Parameter 1</h2>
    <p>Some text about this parameter. See figures</p>

    <figure>
      <img src={{fCO2_flag_piechart_filename}} >
      <figcaption> Figure x: The quality of the measured (and calculated parameters).
      </figcaption>
    </figure>


  <h2>Parameter 2</h2>
    <p>Some text about this parameter. See figures ...</p>


  <!-- ALTERNATIVE: Extract from child template, but this does not work!
  <div id="line_plot">
    {% block line_plot %}{% endblock %}
  </div>

  <div id="line_plot_cleaned">
    {% block line_plot_cleaned %}{% endblock %}
  </div>

  <div id="bar_plot">
    {% block bar_plot %}{% endblock %}
  </div>
  -->

</body>
</html>